<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.plcs.web.wsxd.smsplatform.smsplatformconfig.dao.WsxdSmsConfigDao">
    
	<sql id="wsxdSmsConfigColumns">
		a.id AS "id",
		a.return_state AS "returnState",
		a.msg_count AS "msgCount",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.remarks AS "remarks",
		a.del_flag AS "delFlag"
	</sql>
	
	<sql id="wsxdSmsConfigJoins">
	</sql>
    
	<select id="get" resultType="WsxdSmsConfig">
		SELECT 
			<include refid="wsxdSmsConfigColumns"/>
		FROM wsxd_sms_config a
		<include refid="wsxdSmsConfigJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="WsxdSmsConfig">
		SELECT 
			<include refid="wsxdSmsConfigColumns"/>
		FROM wsxd_sms_config a
		<include refid="wsxdSmsConfigJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="WsxdSmsConfig">
		SELECT 
			<include refid="wsxdSmsConfigColumns"/>
		FROM wsxd_sms_config a
		<include refid="wsxdSmsConfigJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO wsxd_sms_config(
			id,
			return_state,
			msg_count,
			create_by,
			create_date,
			update_by,
			update_date,
			remarks,
			del_flag
		) VALUES (
			#{id},
			#{returnState},
			#{msgCount},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{remarks},
			#{delFlag}
		)
	</insert>
	
	<update id="update">
		UPDATE wsxd_sms_config SET 	
			return_state = #{returnState},
			msg_count = #{msgCount},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		UPDATE wsxd_sms_config SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
</mapper>